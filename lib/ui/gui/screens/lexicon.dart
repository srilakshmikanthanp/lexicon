import 'package:flutter/material.dart';

class Lexicon extends StatelessWidget {
  const Lexicon({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
